#define BOOST_TEST_MODULE HW2TestViewData
#include <boost/test/unit_test.hpp>
#include <sstream>
#include <iostream>
#include "../include/viewdata.hpp"
#include "../include/ip.hpp"

BOOST_AUTO_TEST_SUITE(ViewDataTest) // Начало набора тестов

    BOOST_AUTO_TEST_CASE(test_View1) {
        // hw2::IP ip1;
        // std::istringstream iss1("192.168.1.1");
        // ip1.ReadData(iss1);

        // BOOST_CHECK_EQUAL(ip1.getIP(), "192.168.1.1");
    }

    BOOST_AUTO_TEST_CASE(test_View2) {
        // hw2::IP ip1, ip2;
        // std::istringstream iss1("192.168.1.1");
        // std::istringstream iss2("192.168.1.2");
        // ip1.ReadData(iss1);
        // ip2.ReadData(iss2);

        // BOOST_CHECK(ip1 < ip2); // Проверяем, что ip1 < ip2
        // BOOST_CHECK(!(ip2 < ip1)); // Проверяем обратное
    }

    BOOST_AUTO_TEST_CASE(test_View3) {
        // hw2::IP ip1, ip2;
        // std::istringstream iss1("192.168.1.1");
        // std::istringstream iss2("192.168.1.2");
        // ip1.ReadData(iss1);
        // ip2.ReadData(iss2);

        // BOOST_CHECK(ip1 < ip2); // Проверяем, что ip1 < ip2
        // BOOST_CHECK(!(ip2 < ip1)); // Проверяем обратное
    }

    BOOST_AUTO_TEST_CASE(test_View4) {
        // hw2::IP ip1, ip2;
        // std::istringstream iss1("192.168.1.1");
        // std::istringstream iss2("192.168.1.2");
        // ip1.ReadData(iss1);
        // ip2.ReadData(iss2);

        // BOOST_CHECK(ip1 < ip2); // Проверяем, что ip1 < ip2
        // BOOST_CHECK(!(ip2 < ip1)); // Проверяем обратное
    }

BOOST_AUTO_TEST_SUITE_END() // Конец набора тестов